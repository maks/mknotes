Notes can have optional YAML frontmatter.

The frontmatter can have the following properties:

* title: optional title, if not siupplied, filename is used, with '_" in filename replaced with spaces
* tags: list of tags